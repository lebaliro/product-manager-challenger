export const promptEnrichProduct = (
  nameProduct: string,
): string => `Listar em PT-BR apenas as descrições técnicas e as 
                    principais funcionalidades no presente e o preço estimado do ${nameProduct}`;
